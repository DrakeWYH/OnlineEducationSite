from django.conf.urls import url, include

from . import views as orgViews

urlpatterns = [
    # url('^list/$', orgViews.OrgView.as_view(), name='list'),
    # url('^add_ask/$', orgViews.AddUserAskView.as_view(), name='add_ask'),
]